 <tr class="table table-hover"  >
       <td onMouseOver="this.style.cursor='pointer'" onclick="window.location='<%= book_path(book) %>'"><% if book.cover? %>
      <%= image_tag book.cover_url(:thumb) %>
      <% end %></td>
        <td onMouseOver="this.style.cursor='pointer'" onclick="window.location='<%= book_path(book) %>'"><%= book.author %></td>
        <td onMouseOver="this.style.cursor='pointer'" onclick="window.location='<%= book_path(book) %>'"><%= book.title %></td>
        <td onMouseOver="this.style.cursor='pointer'" onclick="window.location='<%= book_path(book) %>'"><%= book.isbn %></td>
        <td onMouseOver="this.style.cursor='pointer'" onclick="window.location='<%= book_path(book) %>'"><%= book.price %></td>
        
    
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_book_path(book), :class => 'btn btn-mini btn-warning' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      book_path(book),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>




      </tr>